<%- include('../partials/header', { pageTitle, currentType }) %>

<div class="layui-container m-top">
  <div class="layui-card">
    <div class="layui-card-header"><%= title %></div>
    <div class="layui-card-body layui-text">
      <% if (slug === 'shuyuan') { %>
        <p>书源规则建议按以下顺序编写：站点地址、搜索规则、目录规则、正文规则、发现规则。</p>
        <p>发布前请确保 JSON 能被阅读 App 正常导入。</p>
      <% } else if (slug === 'rss') { %>
        <p>RSS 规则建议包含列表入口、标题选择器、时间与正文选择器。</p>
        <p>优先提供稳定地址，避免短期失效的临时链接。</p>
      <% } else if (slug === 'uplink') { %>
        <p>直链上传建议使用 JSON 文件，并确保内容结构可解析。</p>
        <p>如果文件较大，请先在本地校验后再提交。</p>
      <% } else { %>
        <p>暂无文档内容。</p>
      <% } %>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
